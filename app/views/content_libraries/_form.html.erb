<%= form_for(@content_library) do |f| %>
  <% if @content_library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_library.errors.count, "error") %> prohibited this content_library from being saved:</h2>

      <ul>
      <% @content_library.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
